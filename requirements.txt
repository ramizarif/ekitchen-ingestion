# FastAPI and Web Server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.2.0
jinja2>=3.1.0

# Recipe Scraping
recipe-scrapers>=15.8.0
httpx>=0.24.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0

# OpenAI (for video/image parsing and ingredient processing)
openai>=1.0.0

# Image Processing (for video frame extraction)
Pillow>=10.0.0

# Browser impersonation for yt-dlp (bypasses TikTok bot detection)
curl_cffi>=0.7.0

# Ingredient Parsing
ingredient-parser-nlp>=1.0.0

# Spoonacular API (optional)
# Add if needed for ingredient enrichment

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Development
python-dotenv>=1.0.0
